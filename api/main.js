$(document).ready(function () {

    let output = $("#output")
     $.ajax({
        "url": `${firebaseURL}/movies/.json?auth=${idToken}`,
        "method": "GET"
    }).then(results => {
      results.forEach
        })
        return this.cache
    })

})
